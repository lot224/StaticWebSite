  `yarn watch` to spin up the typescript compiler and watch for changes.
  
  `yarn start` to spin up nodemon which will monitor changes to the build folder.

  static assets are served from the web folder